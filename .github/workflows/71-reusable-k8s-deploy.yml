name: Reusable K8s Deploy
on:
  workflow_call:
    inputs:
      namespace:
        required: true
        type: string
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: kubectl -n ${{ inputs.namespace }} apply -f k8s/
